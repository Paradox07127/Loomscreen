import Foundation
import LiveWallpaperCore

/// Phase 3 scaffold placeholder for the LiveWallpaperSharedUI SPM package.
///
/// This package will host the SwiftUI design system + cross-SKU UI
/// components shared by both Lite and Pro: Aerials browser, Bookmarks
/// popover/library, DesignTokens, GlassEffectContainer wrappers,
/// SettingRow / CollapsibleSection / IllustratedEmptyState /
/// LiquidGlassSpinner / etc. (plan §6).
///
/// The placeholder type itself is unused; Phase 3b removes it.
public enum LiveWallpaperSharedUI {
    public static let packageVersion: String = "0.1.0-scaffold"
}
